# Check and fix outbreak related risk factors

checkOutbreaks <- function(access_token) {
  # 1. Case => check CCM_Investigation_Outbreak__c
  # 2. Exposure => check CCM_Exposure_Outbreak__c
  # 3. Get Exposure_Setting_Type__c
  # 4. Check for risk factor -- How to determine role (staff vs. resident)
}
